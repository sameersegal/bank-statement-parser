---
description: Parse brokerage PDF statements into structured JSON
argument-hint: "[path-or-broker, e.g. data/ibkr/train/sept.pdf or ibkr]"
---

Load the `statement-parsing` skill and parse the specified PDF file, directory, or broker dataset into structured JSON.
